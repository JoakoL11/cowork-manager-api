# CoWork Manager API

API RESTful para la gestión de salas y reservas de espacios de trabajo.

## Ejecución
pip install -r requirements.txt

uvicorn main:app --reload --port 3000

## Base URL
http://localhost:3000/api/v1
